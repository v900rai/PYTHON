from math import pi
class sphere:
    radius=5
    def area():
        return 4*pi*sphere.radius*sphere.radius
    def volume():
        return (4/3)*pi*sphere.radius*sphere.radius*sphere.radius

print(f'area of sphere is {sphere.area()}')
print(f'volume of sphere is {sphere.volume()}')